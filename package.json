{
  "name": "pos-terminal",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "A barcode-scanner point of sale terminal that runs in the browser.",
  "scripts": {
    "dev": "vite",
    "start": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
